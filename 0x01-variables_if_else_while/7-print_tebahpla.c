#include <stdio.h>

/**
 * main - Print the lowercase alphabet in reverse,
 * followed by new line
 * Return: Always 0 (Success)
 */

int main(void)
{
	char bh;

	for (bh = 'z', bh >= 'a'; bh--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
