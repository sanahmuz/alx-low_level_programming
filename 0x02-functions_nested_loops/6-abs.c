#include "main.h"

/**
 * _abs - Computes absolute val of integer.
 * @n: interger to be computed.
 *
 * Return: The absolute val of interger.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
